<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('partials/navbar') %>
  <h1><%= title %></h1>
  <p><a href="/pessoas/novo">Nova pessoa</a> | <a href="/">Home</a></p>
  <% if (!pessoas || pessoas.length===0) { %>
    <p>Nenhuma pessoa cadastrada.</p>
  <% } else { %>
    <table>
      <thead><tr><th>ID</th><th>Nome</th><th>CPF</th><th>Telefone</th><th>Email</th></tr></thead>
      <tbody>
      <% pessoas.forEach(p=> { %>
        <tr><td><%= p.id %></td><td><%= p.nome %></td><td><%= p.cpf %></td><td><%= p.telefone %></td><td><%= p.email %></td></tr>
      <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
